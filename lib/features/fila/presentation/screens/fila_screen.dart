import 'package:flutter/material.dart';

class FilaScreen extends StatelessWidget {
  const FilaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      key: Key('fila_screen'),
      body: Center(child: Text('Fila — em breve')),
    );
  }
}
